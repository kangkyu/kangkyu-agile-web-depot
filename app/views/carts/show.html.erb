<p id="notice"><%= notice %></p>

<h2>Cart</h2>
<ul>
  <% @cart.line_items.each do |line_item| %>
    <li><%= line_item.product.title %> <%= number_to_currency(line_item.product.price) %></li>
  <% end %>  
</ul>
<%= link_to 'Edit', edit_cart_path(@cart) %> |
<%= link_to 'Back', carts_path %>
